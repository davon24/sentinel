
build-centos7:
   image: centos:7
   script:
      - echo "Building RPM"
      - pwd
      - ls
      - ls /
   only:
      - tags
   except:
      - branches
   artifacts:
      paths:
         - package/*.rpm

