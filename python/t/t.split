#!/usr/bin/env python3

_repeat = '5m'

import re

reLst = re.split('(\d+)', _repeat)

print(reLst)
for item in reLst:
    if item.isnumeric():
        num = int(item)
    if item.isalpha():
        scale = item

print(num, scale)
  


